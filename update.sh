git add .
git commit -m "Update"
git push origin main

#What to do when local branch is out of sync with the remote branch.
# This usually happens when the remote repository has new commits that you don’t have locally.
#git fetch origin
#git status
#git pull origin main --rebase

